#include <stdio.h>

int main (int argc, char **argv) {
    // int i;
    // for (int i = 1; i < argc; i++) {
    //     printf("argv[%d] = %s ", i, argv[i]);
        
    // }
    // printf("\n");
    // return 0;

    printf("argv[0] = %s", argv[0]);
}